apiVersion: apps/v1
kind: Deployment
metadata:
  name: ntv-12284
  labels:
    app: ntv-12284
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ntv-12284
  template:
    metadata:
      labels:
        app: ntv-12284
    spec:
      hostNetwork: true
      containers:
      - image: localhost:5000/strimer:v2
        name: ntv-12284
        tty: yes
        env:
        - name: "TP-PATH"
          value: ntv-12284
        securityContext:
          privileged: true
        volumeMounts:
        - name: ntv-12284
          mountPath: /etc/luaconf
      volumes:
      - name: ntv-12284
        configMap:
        # Provide the name of the ConfigMap containing the files you want
        # to add to the container
          name: ntv-12284

