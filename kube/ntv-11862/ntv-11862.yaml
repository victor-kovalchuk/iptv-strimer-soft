apiVersion: apps/v1
kind: Deployment
metadata:
  name: ntv-11862
  labels:
    app: ntv-11862
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ntv-11862
  template:
    metadata:
      labels:
        app: ntv-11862
    spec:
      hostNetwork: true
      containers:
      - image: localhost:5000/strimer:v3
        name: ntv-11862
        tty: yes
        env:
        - name: "TP-PATH"
          value: ntv-11862
        securityContext:
          privileged: true
        volumeMounts:
        - name: ntv-11862
          mountPath: /etc/luaconf
      volumes:
      - name: ntv-11862
        configMap:
        # Provide the name of the ConfigMap containing the files you want
        # to add to the container
          name: ntv-11862

