apiVersion: apps/v1
kind: Deployment
metadata:
  name: ntv-11823
  labels:
    app: ntv-11823
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ntv-11823
  template:
    metadata:
      labels:
        app: ntv-11823
    spec:
      hostNetwork: true
      containers:
      - image: localhost:5000/strimer:v7
        name: ntv-11823
        tty: yes
        env:
        - name: "TP-PATH"
          value: ntv-11823
        - name: "DB_ADDR"
          value: "172.17.166.15"
        securityContext:
          privileged: true
        volumeMounts:
        - name: ntv-11823
          mountPath: /etc/luaconf
      volumes:
      - name: ntv-11823
        configMap:
        # Provide the name of the ConfigMap containing the files you want
        # to add to the container
          name: ntv-11823

